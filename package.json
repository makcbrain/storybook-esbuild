{
	"name": "@makcbrain/storybook-monorepo",
	"devDependencies": {
		"@biomejs/biome": "2.2.6",
		"@makcbrain/storybook-framework-react-esbuild": "workspace:*",
		"@storybook/addon-docs": "9.1.15",
		"@storybook/react": "9.1.15",
		"@types/bun": "1.3.0",
		"@types/react": "19.2.2",
		"esbuild": "0.25.11",
		"husky": "9.1.7",
		"react": "19.2.0",
		"react-dom": "19.2.0",
		"storybook": "9.1.15",
		"typescript": "5.9.3"
	},
	"engines": {
		"bun": ">=1.3.0"
	},
	"packageManager": "bun@1.3.0",
	"private": true,
	"type": "module",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"build": "(cd packages/builder-esbuild && bun run build) && (cd packages/framework-react-esbuild && bun run build)",
		"lint": "bunx biome check --write",
		"typecheck": "bunx tsc --noEmit",
		"test": "bun test --no-fail-fast",
		"storybook": "bunx storybook dev -p 6006",
		"build-storybook": "storybook tools",
		"tsl": "bun run build && bun run storybook",
		"reinstallDeps": "rm -rf node_modules packages/builder-esbuild/node_modules packages/framework-react-esbuild/node_modules bun.lock && bun i",
		"precommit": "bun run lint && git add . && bun run typecheck && bun run test",
		"prepare": "husky"
	}
}
